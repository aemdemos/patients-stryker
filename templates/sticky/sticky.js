/**
 * Sticky landing-page template (`template: sticky` metadata). Styling lives in
 * sticky.css; no DOM restructuring needed, so this satisfies the template
 * contract and is a hook for any future sticky-specific decoration.
 * @param {Document} doc The document
 */
// eslint-disable-next-line no-unused-vars
export default async function decorate(doc) {
  // no-op: sticky landing pages are composed entirely from existing blocks.
}
