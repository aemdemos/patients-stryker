{
  "definitions": [
    {
      "title": "Columns",
      "id": "columns",
      "model": "columns",
      "filter": "columns",
      "plugins": {
        "da": {
          "name": "columns",
          "rows": 1,
          "columns": 2,
          "behaviour": "columns"
        }
      }
    },
    {
      "title": "Columns Row",
      "id": "columns-row",
      "model": "columns-row",
      "filter": "columns-row",
      "plugins": {
        "da": {
          "name": "columns-row",
          "behaviour": "columns-row"
        }
      }
    },
    {
      "title": "Columns Cell",
      "id": "columns-cell",
      "model": "columns-cell",
      "filter": "columns-cell",
      "plugins": {
        "da": {
          "unsafeHTML": "<div></div>",
          "behaviour": "columns-cell"
        }
      }
    }
  ],
  "models": [
    {
      "id": "columns",
      "fields": [
        {
          "component": "number",
          "valueType": "number",
          "name": "columns",
          "label": "Number of Columns",
          "value": 2,
          "validation": {
            "numberMin": 1,
            "numberMax": 3
          }
        },
        {
          "component": "text",
          "name": "classes",
          "label": "Layout / column widths (optional)",
          "valueType": "string",
          "description": "Custom column widths: type hyphen-separated proportions, one per column, e.g. 60-40 for two columns or 20-50-30 for three. Numbers are relative and need not add up to 100. Applies on desktop; columns stack on mobile. Leave blank for equal columns. For the icon-grid layout, type: columns-icon-grid"
        }
      ]
    },
    {
      "id": "columns-row",
      "fields": []
    },
    {
      "id": "columns-cell",
      "fields": []
    }
  ],
  "filters": [
    {
      "id": "columns",
      "components": [
        "columns-row"
      ]
    },
    {
      "id": "columns-row",
      "components": []
    },
    {
      "id": "columns-cell",
      "components": [
        "text",
        "image"
      ]
    }
  ]
}
